const BlogSection = () => {
  return (
    <section
      id="blog"
      className="flex min-h-screen w-full flex-col items-center justify-center"
    >
      Blog Section
    </section>
  );
};

export default BlogSection;
